# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'Sharepayment' do
  pod 'SwiftyJSON'
  pod 'Siesta', '= 1.0-beta.8a'
  pod 'Stripe'
  # pod 'SwiftyUserDefaults'
end
